# Phase 1.5 — Complete UI/UX Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Overhaul every visual layer of SOW — design tokens, all page CSS, all component CSS — elevating from functional-but-rough to polished developer tool, while preserving the dark OLED / cyan accent identity.

**Architecture:** Pure CSS redesign (+ minimal TSX cleanup). No new runtime dependencies. Inter + Fira Code already loaded via `next/font` in `layout.tsx`. All emoji replaced with Lucide icons. All inline styles in TSX moved to CSS classes. New token system in `variables.css` propagates to all 16 component CSS files. Zero data-model or behavior changes.

**Tech Stack:** Next.js 16, React 19, Vanilla CSS, Inter (next/font, already configured), lucide-react (already installed)

**Target user:** Single developer/student. References: Linear, GitHub, Notion. Expects information density, zero decorative chrome, crisp dark mode.

---

## File Map

| File | Action | What changes |
|------|--------|-------------|
| `src/styles/variables.css` | **Rewrite** | Full token overhaul: 6-level bg, spacing scale, type scale, shadows, animation vars, status tokens |
| `src/app/globals.css` | **Modify** | Add `.app-layout` / `.app-main` classes; better scrollbar; font-smoothing |
| `src/app/layout.tsx` | **Modify** | Replace 2 inline `style={}` blocks with CSS classes |
| `src/components/layout/WorkspaceSidebar.css` | **Rewrite** | Active left-border indicator; remove inline-style coupling; ws-list, ws-item-row classes |
| `src/components/layout/WorkspaceSidebar.tsx` | **Modify** | Remove all inline styles; replace `···` button with `<MoreHorizontal>`; icon size 16 |
| `src/app/OverviewPage.css` | **Rewrite** | Status badge semantic colors; better section headers; pinned row treatment |
| `src/app/page.tsx` | **Modify** | Remove `📌` → `<Pin>` icon; remove `🔗` → `<Link2>` icon |
| `src/app/workspace/[id]/WorkspacePage.css` | **Rewrite** | Token rename; better split-panel shadow; pill view-toggle |
| `src/app/workspace/[id]/page.tsx` | **Modify** | Remove inline style on title input |
| `src/components/tasks/KanbanBoard.css` | **Rewrite** | Cleaner columns; card depth; priority as top-border; semantic status dots |
| `src/components/tasks/KanbanBoard.tsx` | **Modify** | Remove `🔗` → `<Link2>` icon |
| `src/components/tasks/TaskDetailPanel.css` | **Rewrite** | Pomo pip circles; better meta grid; semantic status/priority badges |
| `src/components/tasks/TaskDetailPanel.tsx` | **Modify** | Remove `🍅` (×2); replace `pomo-tomato` spans with `pomo-pip` spans |
| `src/app/stats/StatsPage.css` | **Rewrite** | Better metric cards; semantic icon wrappers; `--border-default` tokens |
| `src/app/stats/page.tsx` | **Modify** | Remove `🍅` emoji from metric value |
| `src/components/workspace/WorkspaceModal.css` | **Modify** | Token rename only (`--border-color` → `--border-default`, `--accent-cyan` → `--accent`) |
| `src/app/settings/SettingsPage.css` | **Modify** | Token rename |
| `src/components/tasks/QuickAddTask.css` | **Modify** | Token rename + margin tweak |
| `src/components/productivity/PomodoroTimer.css` | **Modify** | Token rename |

---

## Token Rename Reference

Every CSS file must use these new names. Old names are removed from `variables.css`.

| Old | New |
|-----|-----|
| `--border-color` | `--border-default` |
| `--border-focus` | `--border-focus` (same) |
| `--accent-cyan` | `--accent` |
| `--accent-cyan-dim` | `--accent-dim` |
| `--bg-elevated` | `--bg-elevated` (same, value changes) |

---

## Task 1: Design Token Foundation

**Files:**
- Modify: `src/styles/variables.css`
- Modify: `src/app/globals.css`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1.1 — Rewrite `src/styles/variables.css`**

```css
:root {
  /* ── Backgrounds (6-level elevation) ──────────────────── */
  --bg-base:      #0A0A0B;
  --bg-primary:   #111113;
  --bg-secondary: #18181C;
  --bg-tertiary:  #1E1E24;
  --bg-elevated:  #27272F;
  --bg-overlay:   #313139;

  /* ── Text ─────────────────────────────────────────────── */
  --text-primary:   #F2F2F2;
  --text-secondary: #A1A1AA;
  --text-muted:     #52525B;
  --text-disabled:  #3F3F46;

  /* ── Borders ──────────────────────────────────────────── */
  --border-subtle:  #1A1A1F;
  --border-default: #27272F;
  --border-strong:  #3F3F46;
  --border-focus:   rgba(0, 229, 255, 0.45);

  /* ── Accent — Cyan (product decision) ────────────────── */
  --accent:        #00E5FF;
  --accent-dim:    rgba(0, 229, 255, 0.12);
  --accent-subtle: rgba(0, 229, 255, 0.06);

  /* ── Semantic ─────────────────────────────────────────── */
  --danger:         #F87171;
  --danger-dim:     rgba(248, 113, 113, 0.12);
  --success:        #4ADE80;
  --success-dim:    rgba(74, 222, 128, 0.12);
  --warning:        #FCD34D;
  --warning-dim:    rgba(252, 211, 77, 0.12);

  /* ── Status tokens ────────────────────────────────────── */
  --status-todo-color:    var(--text-muted);
  --status-todo-bg:       var(--bg-elevated);
  --status-doing-color:   var(--accent);
  --status-doing-bg:      var(--accent-dim);
  --status-blocked-color: var(--danger);
  --status-blocked-bg:    var(--danger-dim);
  --status-done-color:    var(--success);
  --status-done-bg:       var(--success-dim);

  /* ── Typography Scale ─────────────────────────────────── */
  --text-2xs:  0.625rem;   /* 10px */
  --text-xs:   0.6875rem;  /* 11px */
  --text-sm:   0.75rem;    /* 12px */
  --text-base: 0.8125rem;  /* 13px */
  --text-md:   0.875rem;   /* 14px */
  --text-lg:   1rem;       /* 16px */
  --text-xl:   1.125rem;   /* 18px */
  --text-2xl:  1.375rem;   /* 22px */
  --text-3xl:  1.75rem;    /* 28px */

  /* ── Fonts (set by next/font on body — these are fallbacks) */
  --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-mono: 'Fira Code', ui-monospace, 'SF Mono', Menlo, Consolas, monospace;

  /* ── Spacing (4pt grid) ───────────────────────────────── */
  --space-1:  0.25rem;   /* 4px  */
  --space-2:  0.5rem;    /* 8px  */
  --space-3:  0.75rem;   /* 12px */
  --space-4:  1rem;      /* 16px */
  --space-5:  1.25rem;   /* 20px */
  --space-6:  1.5rem;    /* 24px */
  --space-8:  2rem;      /* 32px */
  --space-10: 2.5rem;    /* 40px */
  --space-12: 3rem;      /* 48px */

  /* ── Border Radius ────────────────────────────────────── */
  --radius-sm:   3px;
  --radius-md:   6px;
  --radius-lg:   8px;
  --radius-xl:   12px;
  --radius-full: 9999px;

  /* ── Shadows ──────────────────────────────────────────── */
  --shadow-sm:   0 1px 2px rgba(0,0,0,0.5);
  --shadow-md:   0 2px 8px rgba(0,0,0,0.6);
  --shadow-lg:   0 8px 24px rgba(0,0,0,0.7);
  --shadow-xl:   0 16px 48px rgba(0,0,0,0.8);
  --shadow-glow: 0 0 0 1px var(--border-focus), 0 0 16px rgba(0,229,255,0.07);

  /* ── Animation ────────────────────────────────────────── */
  --dur-fast: 100ms;
  --dur-base: 180ms;
  --dur-slow: 300ms;
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --ease-in:  cubic-bezier(0.4, 0, 1, 1);
}

[data-theme='light'] {
  --bg-base:      #F4F5F7;
  --bg-primary:   #FFFFFF;
  --bg-secondary: #F0F2F5;
  --bg-tertiary:  #E8EAF0;
  --bg-elevated:  #DDE0E8;
  --bg-overlay:   #D1D5DF;

  --text-primary:   #111827;
  --text-secondary: #4B5563;
  --text-muted:     #9CA3AF;
  --text-disabled:  #D1D5DB;

  --border-subtle:  #EBEBEB;
  --border-default: #E1E4E8;
  --border-strong:  #CDD0D8;
  --border-focus:   rgba(9, 105, 218, 0.45);

  --accent:        #0969DA;
  --accent-dim:    rgba(9, 105, 218, 0.10);
  --accent-subtle: rgba(9, 105, 218, 0.05);

  --danger:         #CF222E;
  --danger-dim:     rgba(207, 34, 46, 0.10);
  --success:        #1A7F37;
  --success-dim:    rgba(26, 127, 55, 0.10);
  --warning:        #9A6700;
  --warning-dim:    rgba(154, 103, 0, 0.10);

  --shadow-sm:   0 1px 2px rgba(0,0,0,0.05);
  --shadow-md:   0 2px 8px rgba(0,0,0,0.08);
  --shadow-lg:   0 4px 16px rgba(0,0,0,0.10);
  --shadow-xl:   0 8px 32px rgba(0,0,0,0.12);
  --shadow-glow: 0 0 0 1px rgba(9,105,218,0.3), 0 0 16px rgba(9,105,218,0.05);
}
```

- [ ] **Step 1.2 — Update `src/app/globals.css`**

```css
@import '../styles/variables.css';

*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html {
  font-size: 16px;
  -webkit-text-size-adjust: 100%;
}

body {
  font-family: var(--font-sans);
  font-size: var(--text-base);
  color: var(--text-primary);
  background-color: var(--bg-base);
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  transition: background-color 0.3s ease, color 0.3s ease;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  cursor: pointer;
  border: none;
  background: none;
  font-family: inherit;
  font-size: inherit;
}

/* ── App Shell ──────────────────────────────────────────── */
.app-layout {
  display: flex;
  height: 100vh;
  overflow: hidden;
  background-color: var(--bg-base);
}

.app-main {
  flex: 1;
  overflow-y: auto;
  background-color: var(--bg-primary);
  min-width: 0;
}

/* ── Scrollbar ──────────────────────────────────────────── */
::-webkit-scrollbar { width: 5px; height: 5px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb {
  background: var(--border-strong);
  border-radius: var(--radius-full);
}
::-webkit-scrollbar-thumb:hover { background: var(--text-muted); }
```

- [ ] **Step 1.3 — Update `src/app/layout.tsx`** — replace the 2 inline style blocks

Find this block:
```tsx
      <body className={`${inter.variable} ${firaCode.variable}`}>
        <div style={{ display: 'flex', height: '100vh', overflow: 'hidden' }}>
          <WorkspaceSidebar />
          <main style={{ flex: 1, overflowY: 'auto', backgroundColor: 'var(--bg-primary)' }}>
            {children}
          </main>
        </div>
      </body>
```

Replace with:
```tsx
      <body className={`${inter.variable} ${firaCode.variable}`}>
        <div className="app-layout">
          <WorkspaceSidebar />
          <main className="app-main">
            {children}
          </main>
        </div>
      </body>
```

- [ ] **Step 1.4 — Verify** — Run `npm run dev`. Open `http://localhost:3000`. Confirm: page loads, dark background is slightly different (near-black with warmth), text is sharp with Inter.

- [ ] **Step 1.5 — Commit**
```bash
git add src/styles/variables.css src/app/globals.css src/app/layout.tsx
git commit -m "feat: design token foundation — 6-level elevation, spacing/type scale, animation vars"
```

---

## Task 2: Sidebar Redesign

**Files:**
- Modify: `src/components/layout/WorkspaceSidebar.css`
- Modify: `src/components/layout/WorkspaceSidebar.tsx`

- [ ] **Step 2.1 — Rewrite `src/components/layout/WorkspaceSidebar.css`**

```css
/* ── Shell ──────────────────────────────────────────────── */
.sidebar {
  width: 220px;
  min-width: 220px;
  height: 100vh;
  background-color: var(--bg-secondary);
  border-right: 1px solid var(--border-subtle);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  flex-shrink: 0;
}

/* ── Header ─────────────────────────────────────────────── */
.sidebar-header {
  padding: var(--space-4) var(--space-4) var(--space-2);
  flex-shrink: 0;
}

.logo {
  font-size: var(--text-xl);
  font-weight: 800;
  letter-spacing: -0.05em;
  color: var(--text-primary);
  user-select: none;
}

.logo-accent {
  color: var(--accent);
}

/* ── Nav ─────────────────────────────────────────────────── */
.sidebar-nav {
  flex: 1;
  overflow-y: auto;
  padding: var(--space-2) var(--space-2) var(--space-3);
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.nav-section-title {
  margin-top: var(--space-5);
  margin-bottom: var(--space-2);
  padding: 0 var(--space-2);
  font-size: var(--text-xs);
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--text-muted);
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.nav-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-3);
  border-radius: var(--radius-md);
  color: var(--text-secondary);
  font-weight: 500;
  font-size: var(--text-base);
  transition: color var(--dur-fast) ease, background-color var(--dur-fast) ease;
  text-decoration: none;
  position: relative;
  cursor: pointer;
}

.nav-item:hover {
  color: var(--text-primary);
  background-color: var(--bg-tertiary);
}

.nav-item.active {
  color: var(--text-primary);
  background-color: var(--bg-tertiary);
  font-weight: 600;
}

/* Left-border accent indicator (VS Code style) */
.nav-item.active::before {
  content: '';
  position: absolute;
  left: 0;
  top: 20%;
  height: 60%;
  width: 2px;
  background: var(--accent);
  border-radius: var(--radius-full);
}

/* ── Workspace list ──────────────────────────────────────── */
.ws-list {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.ws-item-row {
  position: relative;
  display: flex;
  align-items: center;
}

.ws-link {
  flex: 1;
  min-width: 0;
}

.ws-link span {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.ws-emoji {
  font-size: var(--text-base);
  line-height: 1;
  flex-shrink: 0;
}

.add-workspace-btn {
  color: var(--text-muted);
  padding: var(--space-1);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  transition: color var(--dur-fast) ease, background-color var(--dur-fast) ease;
}

.add-workspace-btn:hover {
  color: var(--text-primary);
  background-color: var(--bg-elevated);
}

.ws-edit-btn {
  opacity: 0;
  color: var(--text-muted);
  padding: var(--space-1);
  border-radius: var(--radius-sm);
  transition: opacity var(--dur-fast) ease, background-color var(--dur-fast) ease;
  display: flex;
  align-items: center;
  flex-shrink: 0;
}

.ws-item-row:hover .ws-edit-btn {
  opacity: 1;
}

.ws-edit-btn:hover {
  color: var(--text-primary);
  background-color: var(--bg-elevated);
}

/* ── Footer ─────────────────────────────────────────────── */
.sidebar-footer {
  flex-shrink: 0;
  border-top: 1px solid var(--border-subtle);
  padding: var(--space-2) var(--space-2);
  display: flex;
  flex-direction: column;
  gap: 2px;
}
```

- [ ] **Step 2.2 — Rewrite `src/components/layout/WorkspaceSidebar.tsx`** — remove all inline styles, add `MoreHorizontal` import, rename logo, use new CSS classes

```tsx
'use client';

import { useLiveQuery } from 'dexie-react-hooks';
import Link from 'next/link';
import { usePathname } from 'next/navigation';
import { useState } from 'react';
import { useOrphanRepair } from '@/hooks/useOrphanRepair';
import { db, type Workspace } from '@/db/dexie';
import {
  LayoutDashboard, Briefcase, GraduationCap, User,
  Plus, Settings, BarChart2, MoreHorizontal,
} from 'lucide-react';
import { PomodoroTimer } from '@/components/productivity/PomodoroTimer';
import { ThemeToggle } from '@/components/layout/ThemeToggle';
import { WorkspaceModal } from '@/components/workspace/WorkspaceModal';
import './WorkspaceSidebar.css';

export function WorkspaceSidebar() {
  const pathname = usePathname();
  const workspaces = useLiveQuery(() => db.workspaces.toArray());
  useOrphanRepair();

  const [modalOpen, setModalOpen] = useState(false);
  const [editingWs, setEditingWs] = useState<Workspace | undefined>(undefined);

  const openCreate = () => { setEditingWs(undefined); setModalOpen(true); };
  const openEdit = (ws: Workspace, e: React.MouseEvent) => {
    e.preventDefault();
    e.stopPropagation();
    setEditingWs(ws);
    setModalOpen(true);
  };

  const getIcon = (type: string) => {
    switch (type) {
      case 'work':     return <Briefcase size={16} />;
      case 'studies':  return <GraduationCap size={16} />;
      case 'personal': return <User size={16} />;
      default:         return <Briefcase size={16} />;
    }
  };

  return (
    <>
      <aside className="sidebar">
        <div className="sidebar-header">
          <div className="logo">SO<span className="logo-accent">W</span></div>
        </div>

        <nav className="sidebar-nav">
          <Link href="/" className={`nav-item ${pathname === '/' ? 'active' : ''}`}>
            <LayoutDashboard size={16} />
            <span>Overview</span>
          </Link>

          <Link href="/stats" className={`nav-item ${pathname === '/stats' ? 'active' : ''}`}>
            <BarChart2 size={16} />
            <span>Stats & Focus</span>
          </Link>

          <div className="nav-section-title">
            <span>Workspaces</span>
            <button className="add-workspace-btn" title="Create workspace" onClick={openCreate}>
              <Plus size={13} />
            </button>
          </div>

          <div className="ws-list">
            {workspaces?.map(ws => (
              <div key={ws.id} className="ws-item-row">
                <Link
                  href={`/workspace/${ws.id}`}
                  className={`nav-item ws-link ${pathname === `/workspace/${ws.id}` ? 'active' : ''}`}
                >
                  {ws.icon && ws.icon !== 'folder'
                    ? <span className="ws-emoji">{ws.icon}</span>
                    : getIcon(ws.type)
                  }
                  <span>{ws.name}</span>
                </Link>
                <button
                  onClick={(e) => openEdit(ws, e)}
                  title="Edit workspace"
                  className="ws-edit-btn"
                >
                  <MoreHorizontal size={14} />
                </button>
              </div>
            ))}
          </div>
        </nav>

        <div className="sidebar-footer">
          <PomodoroTimer />
          <ThemeToggle />
          <Link href="/settings" className={`nav-item ${pathname === '/settings' ? 'active' : ''}`}>
            <Settings size={16} />
            <span>Settings</span>
          </Link>
        </div>
      </aside>

      {modalOpen && (
        <WorkspaceModal workspace={editingWs} onClose={() => setModalOpen(false)} />
      )}
    </>
  );
}
```

- [ ] **Step 2.3 — Verify** — Check sidebar in browser. Confirm: active nav item has a 2px cyan left-border. Logo reads "SO**W**" with W in cyan. Workspace edit button hidden until hover. No console errors.

- [ ] **Step 2.4 — Commit**
```bash
git add src/components/layout/WorkspaceSidebar.css src/components/layout/WorkspaceSidebar.tsx
git commit -m "feat: sidebar redesign — accent active indicator, clean nav, remove inline styles"
```

---

## Task 3: Overview Page

**Files:**
- Modify: `src/app/OverviewPage.css`
- Modify: `src/app/page.tsx`

- [ ] **Step 3.1 — Rewrite `src/app/OverviewPage.css`**

```css
/* ── Layout ─────────────────────────────────────────────── */
.overview-container {
  padding: var(--space-8) var(--space-8) var(--space-4);
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.overview-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--space-6);
}

.overview-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.02em;
  margin-bottom: var(--space-1);
}

.overview-subtitle {
  color: var(--text-muted);
  font-size: var(--text-base);
}

/* ── Toolbar ─────────────────────────────────────────────── */
.overview-toolbar {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-6);
}

.search-bar {
  flex: 1;
}

.search-bar input {
  width: 100%;
  max-width: 380px;
  background-color: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-2) var(--space-4);
  color: var(--text-primary);
  font-family: var(--font-sans);
  font-size: var(--text-base);
  outline: none;
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.search-bar input::placeholder { color: var(--text-muted); }

.search-bar input:focus {
  border-color: var(--border-focus);
  box-shadow: var(--shadow-glow);
}

.new-task-btn {
  background-color: var(--accent);
  color: #000;
  border-radius: var(--radius-lg);
  padding: var(--space-2) var(--space-5);
  font-weight: 600;
  font-size: var(--text-base);
  transition: opacity var(--dur-base) ease;
  white-space: nowrap;
}

.new-task-btn:hover { opacity: 0.85; }

/* ── Split layout ────────────────────────────────────────── */
.overview-split-layout {
  flex: 1;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  gap: var(--space-4);
  min-height: 0;
}

.overview-main-view {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  min-width: 0;
  padding-right: var(--space-2);
}

.split-panel {
  flex: 0 0 440px;
  max-width: 48%;
  border-radius: var(--radius-xl);
  overflow: hidden;
  border: 1px solid var(--border-default);
  background-color: var(--bg-secondary);
  display: flex;
  flex-direction: column;
  box-shadow: var(--shadow-lg);
}

/* ── Sections ────────────────────────────────────────────── */
.pinned-section {
  margin-bottom: var(--space-8);
}

.workspaces-section {
  display: flex;
  flex-direction: column;
  gap: var(--space-8);
}

.workspace-group {
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}

.section-title {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-sm);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  color: var(--text-muted);
  margin-bottom: var(--space-3);
  padding-bottom: var(--space-3);
  border-bottom: 1px solid var(--border-subtle);
}

.highlight-ws {
  color: var(--text-secondary);
  text-transform: none;
  letter-spacing: 0;
  font-weight: 500;
  font-size: var(--text-base);
}

.empty-state {
  color: var(--text-muted);
  font-size: var(--text-base);
  padding: var(--space-6) 0;
  text-align: center;
}

/* ── Task list (table style) ─────────────────────────────── */
.tasks-grid {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  overflow: hidden;
  background-color: var(--bg-secondary);
}

.overview-task-row {
  position: relative;
  display: flex;
  align-items: center;
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--border-subtle);
  background-color: transparent;
  transition: background-color var(--dur-fast) ease;
  gap: var(--space-3);
  cursor: default;
}

.overview-task-row:last-child { border-bottom: none; }

.overview-task-row:hover { background-color: var(--bg-tertiary); }

.overview-task-row.pinned {
  box-shadow: inset 2px 0 0 var(--accent);
}

/* ── Priority indicator bar ──────────────────────────────── */
.row-priority-indicator {
  width: 3px;
  height: 20px;
  border-radius: var(--radius-full);
  background-color: var(--border-strong);
  flex-shrink: 0;
}

.row-priority-indicator.priority-high    { background-color: var(--danger); }
.row-priority-indicator.priority-medium  { background-color: var(--warning); }
.row-priority-indicator.priority-low     { background-color: var(--success); }

/* ── Row content ─────────────────────────────────────────── */
.row-main-content {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 2px;
  overflow: hidden;
  min-width: 0;
}

.row-title-area {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  min-width: 0;
}

.row-title-area h3 {
  font-size: var(--text-base);
  font-weight: 500;
  color: var(--text-primary);
  margin: 0;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.row-desc {
  font-size: var(--text-sm);
  color: var(--text-muted);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.row-tags { display: flex; gap: var(--space-1); }

/* ── Meta ────────────────────────────────────────────────── */
.row-meta {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  font-size: var(--text-sm);
  color: var(--text-muted);
  flex-shrink: 0;
}

.row-date {
  display: flex;
  align-items: center;
  gap: 3px;
  white-space: nowrap;
}

/* ── Status badges ───────────────────────────────────────── */
.status-badge {
  font-size: var(--text-xs);
  font-weight: 600;
  padding: 2px var(--space-2);
  border-radius: var(--radius-full);
  text-transform: lowercase;
  white-space: nowrap;
}

.status-badge.todo {
  color: var(--status-todo-color);
  background-color: var(--status-todo-bg);
}

.status-badge.doing {
  color: var(--status-doing-color);
  background-color: var(--status-doing-bg);
}

.status-badge.blocked {
  color: var(--status-blocked-color);
  background-color: var(--status-blocked-bg);
}

.status-badge.done {
  color: var(--status-done-color);
  background-color: var(--status-done-bg);
}

.workspace-badge {
  font-size: var(--text-sm);
  font-weight: 400;
  color: var(--text-muted);
  white-space: nowrap;
}

/* ── Row actions ─────────────────────────────────────────── */
.row-actions {
  display: flex;
  align-items: center;
  gap: var(--space-1);
  opacity: 0;
  transition: opacity var(--dur-fast) ease;
  flex-shrink: 0;
}

.overview-task-row:hover .row-actions { opacity: 1; }

.row-open-link {
  font-size: var(--text-xs);
  padding: 2px var(--space-2);
  background-color: var(--bg-elevated);
  color: var(--accent);
  border-radius: var(--radius-sm);
  font-weight: 500;
  transition: background-color var(--dur-fast) ease;
  white-space: nowrap;
}

.row-open-link:hover { background-color: var(--accent-dim); }

.pin-btn, .delete-quick-btn {
  color: var(--text-muted);
  padding: var(--space-1);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  transition: color var(--dur-fast) ease;
}

.pin-btn:hover { color: var(--text-primary); }
.pin-btn.active { color: var(--accent); }
.delete-quick-btn:hover { color: var(--danger); }

/* ── Tags ────────────────────────────────────────────────── */
.task-card-tag {
  font-size: var(--text-2xs);
  padding: 1px var(--space-2);
  background-color: var(--bg-elevated);
  color: var(--text-muted);
  border-radius: var(--radius-full);
  white-space: nowrap;
}

/* ── Pinned links ────────────────────────────────────────── */
.task-card-pinned-link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-1);
  font-size: var(--text-xs);
  color: var(--accent);
  text-decoration: none;
  background-color: var(--accent-subtle);
  padding: 2px var(--space-2);
  border-radius: var(--radius-sm);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 200px;
  transition: background-color var(--dur-fast) ease;
}

.task-card-pinned-link:hover { background-color: var(--accent-dim); }

/* ── Hover popover ───────────────────────────────────────── */
.task-hover-popover {
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%) translateY(8px);
  width: 260px;
  background-color: var(--bg-overlay);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-3);
  box-shadow: var(--shadow-lg);
  opacity: 0;
  visibility: hidden;
  transition: opacity var(--dur-base) ease, transform var(--dur-base) var(--ease-out);
  z-index: 100;
  pointer-events: none;
}

.overview-task-row:hover .task-hover-popover {
  opacity: 1;
  visibility: visible;
  transform: translateX(-50%) translateY(2px);
  pointer-events: auto;
}

.popover-title {
  font-size: var(--text-base);
  font-weight: 600;
  margin-bottom: var(--space-1);
  color: var(--text-primary);
}

.popover-desc {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin-bottom: var(--space-2);
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.4;
}

.popover-subtitle {
  font-size: var(--text-xs);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: var(--space-2);
  display: block;
}

.popover-subtasks { display: flex; flex-direction: column; gap: 3px; }

.popover-subtask-item {
  font-size: var(--text-sm);
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: 3px var(--space-2);
  border-radius: var(--radius-sm);
  background: var(--bg-elevated);
  cursor: pointer;
  transition: background-color var(--dur-fast) ease;
}

.popover-subtask-item:hover { background: var(--bg-tertiary); }

.status-dot {
  width: 7px;
  height: 7px;
  border-radius: var(--radius-full);
  flex-shrink: 0;
}

.status-dot.todo    { background: var(--status-todo-color); }
.status-dot.doing   { background: var(--status-doing-color); }
.status-dot.blocked { background: var(--status-blocked-color); }
.status-dot.done    { background: var(--status-done-color); }
```

- [ ] **Step 3.2 — Update `src/app/page.tsx`** — replace emojis

First, add `Pin` and `Link2` to the existing lucide import at the top of the file:
```tsx
import { Calendar, Pin, PinOff, Link2 } from 'lucide-react';
```

Then find line 219:
```tsx
<h2 className="section-title">📌 Pinned Tasks</h2>
```
Replace with:
```tsx
<h2 className="section-title"><Pin size={14} /> Pinned Tasks</h2>
```

Then find line 121 (inside the links map):
```tsx
🔗 {link.title || link.url}
```
Replace with:
```tsx
<Link2 size={11} /> {link.title || link.url}
```

- [ ] **Step 3.3 — Verify** — Reload Overview. Confirm: status badges are colored (doing=cyan, done=green, blocked=red), no emoji in section header, row actions fade in on hover, split panel is clean.

- [ ] **Step 3.4 — Commit**
```bash
git add src/app/OverviewPage.css src/app/page.tsx
git commit -m "feat: overview page redesign — semantic status badges, no emoji, clean task rows"
```

---

## Task 4: Workspace Page

**Files:**
- Modify: `src/app/workspace/[id]/WorkspacePage.css`
- Modify: `src/app/workspace/[id]/page.tsx`

- [ ] **Step 4.1 — Rewrite `src/app/workspace/[id]/WorkspacePage.css`**

```css
/* ── Layout ─────────────────────────────────────────────── */
.workspace-container {
  padding: var(--space-8) var(--space-8) var(--space-4);
  display: flex;
  flex-direction: column;
  height: 100vh;
}

.workspace-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--space-5);
  gap: var(--space-4);
}

.workspace-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.02em;
  text-transform: capitalize;
}

.workspace-subtitle {
  color: var(--text-muted);
  font-size: var(--text-base);
  margin-top: var(--space-1);
}

/* ── View toggle (pill style) ────────────────────────────── */
.view-toggles {
  display: flex;
  background-color: var(--bg-tertiary);
  padding: 3px;
  border-radius: var(--radius-lg);
  gap: 2px;
  flex-shrink: 0;
}

.toggle-btn {
  padding: var(--space-1) var(--space-4);
  border-radius: var(--radius-md);
  color: var(--text-muted);
  font-weight: 500;
  font-size: var(--text-base);
  transition: color var(--dur-fast) ease, background-color var(--dur-fast) ease;
}

.toggle-btn:hover { color: var(--text-secondary); }

.toggle-btn.active {
  background-color: var(--bg-elevated);
  color: var(--text-primary);
  box-shadow: var(--shadow-sm);
}

/* ── Toolbar ─────────────────────────────────────────────── */
.workspace-toolbar {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  margin-bottom: var(--space-4);
}

.search-bar { flex: 1; }

.search-bar input {
  width: 100%;
  max-width: 380px;
  background-color: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-2) var(--space-4);
  color: var(--text-primary);
  font-family: var(--font-sans);
  font-size: var(--text-base);
  outline: none;
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.search-bar input::placeholder { color: var(--text-muted); }

.search-bar input:focus {
  border-color: var(--border-focus);
  box-shadow: var(--shadow-glow);
}

.new-task-btn {
  background-color: var(--accent);
  color: #000;
  border-radius: var(--radius-lg);
  padding: var(--space-2) var(--space-5);
  font-weight: 600;
  font-size: var(--text-base);
  transition: opacity var(--dur-base) ease;
  white-space: nowrap;
}

.new-task-btn:hover { opacity: 0.85; }

/* ── Title edit input ────────────────────────────────────── */
.workspace-title-input {
  font-size: var(--text-2xl);
  font-weight: 700;
  letter-spacing: -0.02em;
  background: transparent;
  border: 1px solid var(--border-focus);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  outline: none;
  padding: 0 var(--space-2);
  font-family: var(--font-sans);
  box-shadow: var(--shadow-glow);
}

/* ── Content area ────────────────────────────────────────── */
.workspace-content {
  flex: 1;
  overflow: hidden;
  display: flex;
  flex-direction: row;
  gap: var(--space-4);
  min-height: 0;
}

.main-view {
  flex: 1;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.split-panel {
  flex: 0 0 440px;
  max-width: 48%;
  border-radius: var(--radius-xl);
  overflow: hidden;
  border: 1px solid var(--border-default);
  background-color: var(--bg-secondary);
  display: flex;
  flex-direction: column;
  box-shadow: var(--shadow-lg);
}
```

- [ ] **Step 4.2 — Update `src/app/workspace/[id]/page.tsx`** — remove inline style on title input

Find the title input element (around line 76):
```tsx
              style={{ fontSize: '2rem', fontWeight: 'bold', background: 'transparent', border: '1px solid var(--border-color)', color: 'var(--text-primary)', outline: 'none', borderRadius: '4px', padding: '0 0.5rem' }}
```
Remove the entire `style={{...}}` attribute from that `<input>` element. The new `.workspace-title-input` class in CSS handles all styling.

Also ensure the input has `className="workspace-title-input"`:
```tsx
              <input
                autoFocus
                className="workspace-title-input"
                value={editedName}
                onChange={(e) => setEditedName(e.target.value)}
                onBlur={handleRenameWorkspace}
                onKeyDown={(e) => e.key === 'Enter' && handleRenameWorkspace()}
              />
```

- [ ] **Step 4.3 — Verify** — Open a workspace. Confirm: view toggle is a pill selector, title is large with letter-spacing, search input focuses with cyan glow, split panel shadow is visible.

- [ ] **Step 4.4 — Commit**
```bash
git add src/app/workspace/[id]/WorkspacePage.css src/app/workspace/[id]/page.tsx
git commit -m "feat: workspace page redesign — pill toggle, clean toolbar, remove inline styles"
```

---

## Task 5: Kanban Board

**Files:**
- Modify: `src/components/tasks/KanbanBoard.css`
- Modify: `src/components/tasks/KanbanBoard.tsx`

- [ ] **Step 5.1 — Rewrite `src/components/tasks/KanbanBoard.css`**

```css
/* ── Board ───────────────────────────────────────────────── */
.kanban-board {
  display: flex;
  gap: var(--space-4);
  height: 100%;
  overflow-x: auto;
  padding-bottom: var(--space-4);
  align-items: flex-start;
}

/* ── Column ──────────────────────────────────────────────── */
.kanban-column {
  flex: 0 0 288px;
  display: flex;
  flex-direction: column;
  background-color: var(--bg-tertiary);
  border-radius: var(--radius-lg);
  max-height: 100%;
  border: 1px solid var(--border-subtle);
}

.column-title {
  padding: var(--space-3) var(--space-4);
  font-size: var(--text-sm);
  font-weight: 600;
  color: var(--text-secondary);
  display: flex;
  justify-content: space-between;
  align-items: center;
  border-bottom: 1px solid var(--border-subtle);
  text-transform: uppercase;
  letter-spacing: 0.06em;
}

.task-count {
  font-size: var(--text-xs);
  font-weight: 500;
  color: var(--text-muted);
  background-color: var(--bg-elevated);
  padding: 1px var(--space-2);
  border-radius: var(--radius-full);
  border: 1px solid var(--border-default);
}

.column-content {
  padding: var(--space-2);
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  transition: background-color var(--dur-base) ease;
}

.column-content.dragging-over {
  background-color: var(--bg-elevated);
}

/* ── Card ────────────────────────────────────────────────── */
.task-card {
  position: relative;
  background-color: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  padding: var(--space-3);
  cursor: grab;
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.task-card:hover {
  border-color: var(--border-strong);
  box-shadow: var(--shadow-md);
}

.task-card:active { cursor: grabbing; }

.task-card.dragging {
  box-shadow: var(--shadow-xl);
  border-color: var(--accent);
  opacity: 0.96;
  rotate: 1.5deg;
}

/* Priority indicator as top border stripe */
.priority-high   { border-top: 2px solid var(--danger); }
.priority-medium { border-top: 2px solid var(--warning); }
.priority-low    { border-top: 2px solid var(--success); }

/* ── Card content ────────────────────────────────────────── */
.task-header h4 {
  font-size: var(--text-base);
  color: var(--text-primary);
  font-weight: 500;
  line-height: 1.4;
  margin-bottom: var(--space-1);
}

.task-desc {
  font-size: var(--text-sm);
  color: var(--text-muted);
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.4;
}

.task-card-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 3px;
  margin-top: var(--space-2);
}

.task-card-tag {
  font-size: var(--text-2xs);
  padding: 1px var(--space-2);
  background-color: var(--bg-elevated);
  color: var(--text-muted);
  border-radius: var(--radius-full);
}

.task-card-pinned-link {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  font-size: var(--text-xs);
  color: var(--accent);
  text-decoration: none;
  background-color: var(--accent-subtle);
  padding: 2px var(--space-2);
  border-radius: var(--radius-sm);
  margin-top: var(--space-2);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 100%;
  transition: background-color var(--dur-fast) ease;
}

.task-card-pinned-link:hover { background-color: var(--accent-dim); }

/* ── Status dots ─────────────────────────────────────────── */
.status-dot {
  width: 7px;
  height: 7px;
  border-radius: var(--radius-full);
  flex-shrink: 0;
}

.status-dot.todo    { background: var(--status-todo-color); }
.status-dot.doing   { background: var(--status-doing-color); }
.status-dot.blocked { background: var(--status-blocked-color); }
.status-dot.done    { background: var(--status-done-color); }

/* ── Hover popover ───────────────────────────────────────── */
.task-hover-popover {
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%) translateY(8px);
  width: 240px;
  background-color: var(--bg-overlay);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-3);
  box-shadow: var(--shadow-lg);
  opacity: 0;
  visibility: hidden;
  transition: opacity var(--dur-base) ease, transform var(--dur-base) var(--ease-out);
  z-index: 100;
  pointer-events: none;
}

.task-card:hover .task-hover-popover {
  opacity: 1;
  visibility: visible;
  transform: translateX(-50%) translateY(2px);
  pointer-events: auto;
}

.popover-title {
  font-size: var(--text-base);
  font-weight: 600;
  margin-bottom: var(--space-1);
  color: var(--text-primary);
}

.popover-desc {
  font-size: var(--text-sm);
  color: var(--text-secondary);
  margin-bottom: var(--space-2);
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  overflow: hidden;
  line-height: 1.4;
}

.popover-subtitle {
  font-size: var(--text-xs);
  text-transform: uppercase;
  letter-spacing: 0.06em;
  color: var(--text-muted);
  margin-bottom: var(--space-2);
  display: block;
}

.popover-subtasks { display: flex; flex-direction: column; gap: 3px; }

.popover-subtask-item {
  font-size: var(--text-sm);
  color: var(--text-primary);
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: 3px var(--space-2);
  border-radius: var(--radius-sm);
  background: var(--bg-elevated);
  cursor: pointer;
  transition: background-color var(--dur-fast) ease;
}

.popover-subtask-item:hover { background: var(--bg-tertiary); }
```

- [ ] **Step 5.2 — Update `src/components/tasks/KanbanBoard.tsx`** — replace `🔗` emoji

Add `Link2` to the existing lucide import:
```tsx
import { ..., Link2 } from 'lucide-react';
```

Find line 120:
```tsx
                              🔗 {link.title || link.url}
```
Replace with:
```tsx
                              <Link2 size={10} /> {link.title || link.url}
```

- [ ] **Step 5.3 — Verify** — Open a workspace in Kanban view. Confirm: columns have subtle bg, cards have priority top-border, card hover shows popover, dragging card rotates slightly with cyan border.

- [ ] **Step 5.4 — Commit**
```bash
git add src/components/tasks/KanbanBoard.css src/components/tasks/KanbanBoard.tsx
git commit -m "feat: kanban redesign — priority top-border, card depth, no emoji"
```

---

## Task 6: Task Detail Panel

**Files:**
- Modify: `src/components/tasks/TaskDetailPanel.css`
- Modify: `src/components/tasks/TaskDetailPanel.tsx`

- [ ] **Step 6.1 — Rewrite `src/components/tasks/TaskDetailPanel.css`**

```css
/* ── Shell ───────────────────────────────────────────────── */
.task-detail-panel {
  display: flex;
  flex-direction: column;
  height: 100%;
  background-color: var(--bg-secondary);
  animation: slideInRight var(--dur-slow) var(--ease-out);
}

@keyframes slideInRight {
  from { transform: translateX(100%); opacity: 0; }
  to   { transform: translateX(0);    opacity: 1; }
}

/* ── Breadcrumbs ─────────────────────────────────────────── */
.task-breadcrumbs {
  padding: var(--space-2) var(--space-6);
  display: flex;
  align-items: center;
  gap: var(--space-2);
  border-bottom: 1px solid var(--border-subtle);
  background-color: var(--bg-tertiary);
  flex-shrink: 0;
}

.breadcrumb-link {
  font-size: var(--text-sm);
  color: var(--text-muted);
  cursor: pointer;
  transition: color var(--dur-fast) ease;
  background: none;
  border: none;
  padding: 0;
  font-family: var(--font-sans);
}

.breadcrumb-link:hover { color: var(--text-secondary); }
.breadcrumb-sep { color: var(--text-disabled); font-size: var(--text-xs); }
.breadcrumb-current { font-size: var(--text-sm); color: var(--text-secondary); font-weight: 500; }

/* ── Panel header ────────────────────────────────────────── */
.panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--space-4) var(--space-6);
  border-bottom: 1px solid var(--border-subtle);
  flex-shrink: 0;
}

.header-actions {
  display: flex;
  gap: var(--space-2);
  align-items: center;
}

/* ── Status / priority badges ────────────────────────────── */
.status-badge {
  font-size: var(--text-xs);
  font-weight: 600;
  padding: 2px var(--space-2);
  border-radius: var(--radius-full);
  text-transform: lowercase;
  background: none;
  border: none;
  cursor: pointer;
  font-family: var(--font-sans);
  transition: opacity var(--dur-fast) ease;
}

.status-badge:hover { opacity: 0.75; }

.status-badge.todo    { color: var(--status-todo-color);    background-color: var(--status-todo-bg); }
.status-badge.doing   { color: var(--status-doing-color);   background-color: var(--status-doing-bg); }
.status-badge.blocked { color: var(--status-blocked-color); background-color: var(--status-blocked-bg); }
.status-badge.done    { color: var(--status-done-color);    background-color: var(--status-done-bg); }

.priority-badge {
  font-size: var(--text-xs);
  font-weight: 600;
  padding: 2px var(--space-2);
  border-radius: var(--radius-full);
  text-transform: lowercase;
  background-color: var(--bg-elevated);
  border: none;
  color: var(--text-secondary);
  cursor: pointer;
  font-family: var(--font-sans);
  transition: background-color var(--dur-fast) ease;
  appearance: none;
  outline: none;
}

.priority-badge:hover { background-color: var(--bg-overlay); }

.action-icon-btn {
  color: var(--text-muted);
  transition: color var(--dur-fast) ease;
  padding: var(--space-1);
  background: transparent;
  border: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  border-radius: var(--radius-sm);
}

.action-icon-btn:hover { color: var(--text-primary); background-color: var(--bg-elevated); }
.delete-task-btn:hover { color: var(--danger); background-color: var(--danger-dim); }

/* ── Content ─────────────────────────────────────────────── */
.panel-content {
  flex: 1;
  overflow-y: auto;
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-6);
}

/* ── Title ───────────────────────────────────────────────── */
.task-title-input {
  font-size: var(--text-xl);
  font-weight: 600;
  color: var(--text-primary);
  background: transparent;
  border: none;
  width: 100%;
  outline: none;
  font-family: var(--font-sans);
  letter-spacing: -0.01em;
  line-height: 1.3;
}

.task-title-input::placeholder { color: var(--text-disabled); }

/* ── Meta grid ───────────────────────────────────────────── */
.task-meta-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-2);
}

.meta-item {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  font-size: var(--text-sm);
  color: var(--text-secondary);
  background-color: var(--bg-tertiary);
  padding: var(--space-1) var(--space-3);
  border-radius: var(--radius-full);
  cursor: pointer;
  transition: background-color var(--dur-fast) ease;
  border: 1px solid var(--border-subtle);
}

.meta-item:hover { background-color: var(--bg-elevated); }

.date-input {
  background: transparent;
  border: none;
  color: var(--text-secondary);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  outline: none;
  cursor: pointer;
}

.date-input::-webkit-calendar-picker-indicator {
  filter: invert(0.5);
  cursor: pointer;
}

.tag-input {
  background: transparent;
  border: none;
  color: var(--text-secondary);
  font-family: var(--font-sans);
  font-size: var(--text-sm);
  outline: none;
  width: 160px;
}

/* ── Sections ────────────────────────────────────────────── */
.task-section { display: flex; flex-direction: column; gap: var(--space-2); }

.task-section h3 {
  font-size: var(--text-xs);
  font-weight: 600;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.07em;
}

/* ── Description ─────────────────────────────────────────── */
.task-description-editor {
  width: 100%;
  min-height: 100px;
  background-color: var(--bg-primary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  padding: var(--space-3);
  color: var(--text-primary);
  font-family: var(--font-sans);
  font-size: var(--text-base);
  line-height: 1.6;
  resize: vertical;
  outline: none;
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.task-description-editor:focus {
  border-color: var(--border-focus);
  box-shadow: var(--shadow-glow);
}

/* ── Subtasks ────────────────────────────────────────────── */
.subtasks-list {
  display: flex;
  flex-direction: column;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  background-color: var(--bg-primary);
  overflow: hidden;
}

.subtask-item {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-2) var(--space-3);
  border-bottom: 1px solid var(--border-subtle);
}

.subtask-item:last-child { border-bottom: none; }
.subtask-item:hover .subtask-delete { opacity: 1; }

.subtask-check {
  width: 16px;
  height: 16px;
  border: 1.5px solid var(--border-strong);
  border-radius: var(--radius-sm);
  display: flex;
  align-items: center;
  justify-content: center;
  color: transparent;
  transition: all var(--dur-base) ease;
  flex-shrink: 0;
  cursor: pointer;
}

.subtask-item.completed .subtask-check {
  background-color: var(--accent);
  border-color: var(--accent);
  color: #000;
}

.subtask-title-input {
  flex: 1;
  font-size: var(--text-base);
  color: var(--text-primary);
  background: transparent;
  border: none;
  outline: none;
  font-family: var(--font-sans);
}

.subtask-title-input.completed {
  text-decoration: line-through;
  color: var(--text-muted);
}

.subtask-navigate {
  color: var(--text-muted);
  padding: var(--space-1);
  border-radius: var(--radius-sm);
  transition: all var(--dur-fast) ease;
  display: flex;
  align-items: center;
}

.subtask-navigate:hover { color: var(--accent); background-color: var(--accent-subtle); }

.subtask-delete {
  opacity: 0;
  color: var(--danger);
  transition: opacity var(--dur-fast) ease;
  display: flex;
  align-items: center;
  padding: var(--space-1);
}

.subtask-input-wrapper {
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding: var(--space-2) var(--space-3);
}

.subtask-input-wrapper input {
  flex: 1;
  background: transparent;
  border: none;
  color: var(--text-secondary);
  font-family: var(--font-sans);
  font-size: var(--text-base);
  outline: none;
}

/* ── Embeds ──────────────────────────────────────────────── */
.embed-section { display: flex; flex-direction: column; gap: var(--space-3); }

.embed-url-input {
  background-color: var(--bg-primary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  padding: var(--space-2) var(--space-3);
  color: var(--text-primary);
  outline: none;
  font-family: var(--font-sans);
  font-size: var(--text-base);
  transition: border-color var(--dur-base) ease;
  width: 100%;
}

.embed-url-input:focus { border-color: var(--border-focus); }

.embed-frame-wrapper {
  position: relative;
  width: 100%;
  padding-bottom: 56.25%;
  height: 0;
  overflow: hidden;
  border-radius: var(--radius-md);
  border: 1px solid var(--border-default);
}

.embed-frame { position: absolute; top: 0; left: 0; width: 100%; height: 100%; border: none; }

.embed-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-3);
  background-color: var(--bg-primary);
  border: 1px dashed var(--border-strong);
  border-radius: var(--radius-lg);
  padding: var(--space-10) var(--space-4);
  color: var(--text-muted);
  text-align: center;
  cursor: pointer;
  transition: border-color var(--dur-base) ease, color var(--dur-base) ease;
}

.embed-placeholder:hover { border-color: var(--text-muted); color: var(--text-secondary); }

/* ── Pomodoro ────────────────────────────────────────────── */
.pomodoro-section {
  background: var(--bg-primary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: var(--space-4);
}

.pomodoro-counter {
  display: flex;
  align-items: center;
  gap: var(--space-4);
  margin-bottom: var(--space-3);
}

.pomo-adj-btn {
  background: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  color: var(--text-primary);
  width: 26px;
  height: 26px;
  border-radius: var(--radius-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: var(--text-md);
  transition: background-color var(--dur-fast) ease;
}

.pomo-adj-btn:hover { background: var(--bg-elevated); }

.pomo-count-display {
  font-size: var(--text-xl);
  font-weight: 700;
  font-family: var(--font-mono);
  color: var(--text-primary);
}

/* Pip circles replace emoji tomatoes */
.pomodoro-visual-list {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-1);
  align-items: center;
}

.pomo-pip {
  display: inline-block;
  width: 8px;
  height: 8px;
  background: var(--accent);
  border-radius: var(--radius-full);
  opacity: 0.7;
}

/* ── Links ───────────────────────────────────────────────── */
.links-section { display: flex; flex-direction: column; gap: var(--space-2); }
.links-list    { display: flex; flex-direction: column; gap: var(--space-2); }

.link-item {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: var(--space-2) var(--space-3);
  background-color: var(--bg-primary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  transition: border-color var(--dur-fast) ease;
}

.link-item.pinned {
  border-color: var(--accent);
  box-shadow: 0 0 0 1px var(--accent-dim);
}

.link-title {
  color: var(--text-primary);
  text-decoration: none;
  font-size: var(--text-base);
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  max-width: 200px;
}

.link-title:hover { text-decoration: underline; }

.link-actions { display: flex; gap: var(--space-1); }

.link-actions button {
  background: transparent;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  padding: var(--space-1);
  display: flex;
  align-items: center;
  border-radius: var(--radius-sm);
  transition: color var(--dur-fast) ease, background-color var(--dur-fast) ease;
}

.link-actions button:hover { color: var(--text-primary); background-color: var(--bg-elevated); }
.delete-link-btn:hover     { color: var(--danger) !important; }
.pin-btn.active            { color: var(--warning); }
```

- [ ] **Step 6.2 — Update `src/components/tasks/TaskDetailPanel.tsx`** — remove both 🍅 emojis

Find line 211:
```tsx
              {task.pomodoroSessions || 0} 🍅
```
Replace with:
```tsx
              {task.pomodoroSessions || 0}
```

Find line 222:
```tsx
              <span key={i} className="pomo-tomato">🍅</span>
```
Replace with:
```tsx
              <span key={i} className="pomo-pip" />
```

- [ ] **Step 6.3 — Verify** — Click a task to open the panel. Confirm: panel slides in, pomodoro shows small cyan dot pips instead of tomato emoji, status badge is colored, description textarea focuses with cyan glow.

- [ ] **Step 6.4 — Commit**
```bash
git add src/components/tasks/TaskDetailPanel.css src/components/tasks/TaskDetailPanel.tsx
git commit -m "feat: task panel redesign — pomo pip circles, semantic badges, no emoji"
```

---

## Task 7: Stats Page

**Files:**
- Modify: `src/app/stats/StatsPage.css`
- Modify: `src/app/stats/page.tsx`

- [ ] **Step 7.1 — Rewrite `src/app/stats/StatsPage.css`**

```css
.stats-container {
  padding: var(--space-8);
  max-width: 960px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: var(--space-8);
}

.stats-header { display: flex; flex-direction: column; gap: var(--space-1); }

.stats-title {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.02em;
}

.stats-subtitle {
  color: var(--text-muted);
  font-size: var(--text-base);
}

/* ── Metrics grid ────────────────────────────────────────── */
.metrics-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: var(--space-4);
}

.metric-card {
  background-color: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xl);
  padding: var(--space-5);
  display: flex;
  align-items: center;
  gap: var(--space-4);
  transition: border-color var(--dur-base) ease;
}

.metric-card:hover { border-color: var(--border-strong); }

.metric-icon-wrapper {
  width: 44px;
  height: 44px;
  border-radius: var(--radius-lg);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.metric-icon-wrapper.success  { background-color: var(--success-dim);  color: var(--success); }
.metric-icon-wrapper.danger   { background-color: var(--danger-dim);   color: var(--danger); }
.metric-icon-wrapper.primary  { background-color: var(--accent-dim);   color: var(--accent); }
.metric-icon-wrapper.warning  { background-color: var(--warning-dim);  color: var(--warning); }

.metric-info h3 {
  font-size: var(--text-xs);
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.07em;
  font-weight: 600;
  margin-bottom: var(--space-1);
}

.metric-value {
  font-size: var(--text-3xl);
  font-weight: 700;
  color: var(--text-primary);
  font-variant-numeric: tabular-nums;
  letter-spacing: -0.02em;
}

/* ── Charts section ──────────────────────────────────────── */
.charts-section { display: flex; flex-direction: column; gap: var(--space-5); }

.chart-card {
  background-color: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xl);
  padding: var(--space-6);
}

.chart-card h3 {
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: var(--space-5);
  padding-bottom: var(--space-3);
  border-bottom: 1px solid var(--border-subtle);
}

/* ── Bar chart ───────────────────────────────────────────── */
.simple-bar-chart { display: flex; flex-direction: column; gap: var(--space-3); }

.bar-row { display: flex; align-items: center; gap: var(--space-4); }

.bar-label {
  width: 72px;
  font-size: var(--text-sm);
  color: var(--text-secondary);
  text-align: right;
}

.bar-track {
  flex: 1;
  height: 8px;
  background-color: var(--bg-elevated);
  border-radius: var(--radius-full);
  overflow: hidden;
}

.bar-fill {
  height: 100%;
  border-radius: var(--radius-full);
  transition: width 0.6s var(--ease-out);
}

.bar-fill.priority-high   { background-color: var(--danger); }
.bar-fill.priority-medium { background-color: var(--warning); }
.bar-fill.priority-low    { background-color: var(--success); }

.bar-count {
  width: 28px;
  font-weight: 600;
  font-size: var(--text-base);
  color: var(--text-primary);
  text-align: right;
  font-variant-numeric: tabular-nums;
}
```

- [ ] **Step 7.2 — Update `src/app/stats/page.tsx`** — remove 🍅 emoji

Find line 46:
```tsx
            <p className="metric-value">{totalPomodoros} 🍅</p>
```
Replace with:
```tsx
            <p className="metric-value">{totalPomodoros}</p>
```

- [ ] **Step 7.3 — Verify** — Open `/stats`. Confirm: metric cards have semantic icon colors (success=green, primary=cyan), bar chart track is thin 8px, no emoji in pomodoro count.

- [ ] **Step 7.4 — Commit**
```bash
git add src/app/stats/StatsPage.css src/app/stats/page.tsx
git commit -m "feat: stats page redesign — semantic metric cards, clean bar chart, no emoji"
```

---

## Task 8: Supporting Components

**Files:**
- Modify: `src/components/workspace/WorkspaceModal.css`
- Modify: `src/app/settings/SettingsPage.css`
- Modify: `src/components/tasks/QuickAddTask.css`
- Modify: `src/components/productivity/PomodoroTimer.css`

These files have correct structure but use old token names. Replace tokens + minor polish.

- [ ] **Step 8.1 — Update `src/components/workspace/WorkspaceModal.css`** — token rename only

Replace all occurrences:
- `var(--border-color)` → `var(--border-default)`
- `var(--accent-cyan)` → `var(--accent)`

Also improve modal-overlay scrim and add animation:
```css
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
  animation: fadeIn var(--dur-base) ease;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to   { opacity: 1; }
}

.modal-box {
  background: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xl);
  width: 420px;
  max-width: calc(100vw - 2rem);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: var(--shadow-xl);
  animation: slideUp var(--dur-slow) var(--ease-out);
}

@keyframes slideUp {
  from { transform: translateY(12px); opacity: 0; }
  to   { transform: translateY(0);    opacity: 1; }
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: var(--space-4) var(--space-6);
  border-bottom: 1px solid var(--border-subtle);
}

.modal-header h2 {
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
}

.modal-close-btn {
  background: none;
  border: none;
  color: var(--text-muted);
  cursor: pointer;
  padding: var(--space-1);
  border-radius: var(--radius-sm);
  display: flex;
  transition: color var(--dur-fast) ease, background-color var(--dur-fast) ease;
}

.modal-close-btn:hover { color: var(--text-primary); background: var(--bg-elevated); }

.modal-body {
  padding: var(--space-5) var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.modal-label {
  font-size: var(--text-xs);
  color: var(--text-muted);
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.07em;
  margin-bottom: calc(-1 * var(--space-2));
}

.modal-input {
  background: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-base);
  font-family: var(--font-sans);
  outline: none;
  width: 100%;
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.modal-input:focus { border-color: var(--border-focus); box-shadow: var(--shadow-glow); }

.modal-icon-grid {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-1);
}

.icon-option {
  background: var(--bg-tertiary);
  border: 2px solid transparent;
  border-radius: var(--radius-md);
  padding: var(--space-1) var(--space-2);
  font-size: var(--text-lg);
  cursor: pointer;
  transition: border-color var(--dur-fast) ease;
}

.icon-option:hover, .icon-option.selected { border-color: var(--accent); }

.modal-color-grid { display: flex; gap: var(--space-2); flex-wrap: wrap; }

.color-swatch {
  width: 26px;
  height: 26px;
  border-radius: var(--radius-full);
  border: 2px solid transparent;
  cursor: pointer;
  transition: transform var(--dur-fast) ease, border-color var(--dur-fast) ease;
}

.color-swatch:hover { transform: scale(1.15); }
.color-swatch.selected { border-color: var(--text-primary); outline: 2px solid var(--bg-secondary); outline-offset: 1px; }

.modal-footer {
  display: flex;
  align-items: center;
  gap: var(--space-2);
  padding: var(--space-4) var(--space-6);
  border-top: 1px solid var(--border-subtle);
}

.modal-btn {
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-md);
  font-size: var(--text-base);
  font-weight: 500;
  cursor: pointer;
  border: 1px solid transparent;
  font-family: var(--font-sans);
  transition: opacity var(--dur-fast) ease, background-color var(--dur-fast) ease;
}

.modal-btn:disabled { opacity: 0.4; cursor: not-allowed; }
.modal-btn.primary { background: var(--accent); color: #000; }
.modal-btn.primary:hover:not(:disabled) { opacity: 0.85; }
.modal-btn.ghost { background: transparent; border-color: var(--border-default); color: var(--text-secondary); }
.modal-btn.ghost:hover { color: var(--text-primary); border-color: var(--border-focus); }
.modal-btn.danger { background: transparent; border-color: var(--danger); color: var(--danger); }
.modal-btn.danger:hover { background: var(--danger); color: #fff; }
```

- [ ] **Step 8.2 — Update `src/app/settings/SettingsPage.css`** — token rename

Replace:
- `var(--border-color)` → `var(--border-default)`
- `var(--accent-cyan)` → `var(--accent)`

Full file after rename:
```css
.settings-container {
  max-width: 680px;
  margin: 0 auto;
  padding: var(--space-8) var(--space-6);
}

.settings-header h1 {
  font-size: var(--text-2xl);
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: -0.02em;
  margin: 0 0 var(--space-1);
}

.settings-subtitle {
  color: var(--text-muted);
  font-size: var(--text-base);
  margin: 0 0 var(--space-8);
}

.settings-sections { display: flex; flex-direction: column; gap: var(--space-5); }

.settings-section {
  background: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xl);
  padding: var(--space-6);
  display: flex;
  flex-direction: column;
  gap: var(--space-4);
}

.settings-section h2 {
  font-size: var(--text-base);
  font-weight: 600;
  color: var(--text-primary);
  margin: 0;
  padding-bottom: var(--space-3);
  border-bottom: 1px solid var(--border-subtle);
}

.settings-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-4);
}

.settings-row > div strong {
  display: block;
  color: var(--text-primary);
  font-size: var(--text-base);
  font-weight: 500;
}

.settings-row > div p {
  color: var(--text-muted);
  font-size: var(--text-sm);
  margin: 2px 0 0;
}

.settings-select, .settings-input {
  background: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-base);
  font-family: var(--font-sans);
  outline: none;
  min-width: 180px;
  transition: border-color var(--dur-base) ease;
}

.settings-input:focus, .settings-select:focus { border-color: var(--border-focus); }

.settings-btn {
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-md);
  font-size: var(--text-base);
  font-weight: 500;
  cursor: pointer;
  border: none;
  font-family: var(--font-sans);
  transition: opacity var(--dur-fast) ease;
}

.settings-btn.primary { background: var(--accent); color: #000; }
.settings-btn.primary:hover { opacity: 0.85; }
.settings-btn:disabled { opacity: 0.4; cursor: not-allowed; }

.settings-hint {
  color: var(--text-muted);
  font-size: var(--text-sm);
  margin: 0;
}

.settings-hint.coming-soon { color: var(--text-disabled); font-style: italic; }
.settings-label { color: var(--text-secondary); font-size: var(--text-base); }
.settings-value { color: var(--text-primary); font-size: var(--text-base); }

.settings-value code {
  background: var(--bg-elevated);
  padding: 1px var(--space-2);
  border-radius: var(--radius-sm);
  font-family: var(--font-mono);
  font-size: var(--text-sm);
}
```

- [ ] **Step 8.3 — Update `src/components/tasks/QuickAddTask.css`** — token rename

Replace `var(--border-color)` → `var(--border-default)` and `var(--accent-cyan)` → `var(--accent)`:
```css
.quick-add-form {
  display: flex;
  gap: var(--space-3);
  align-items: center;
  background-color: var(--bg-secondary);
  border: 1px solid var(--border-default);
  border-radius: var(--radius-xl);
  padding: var(--space-2);
  margin-bottom: var(--space-4);
  transition: border-color var(--dur-base) ease, box-shadow var(--dur-base) ease;
}

.quick-add-form:focus-within {
  border-color: var(--border-focus);
  box-shadow: var(--shadow-glow);
}

.input-wrapper {
  flex: 1;
  display: flex;
  align-items: center;
  gap: var(--space-3);
  padding-left: var(--space-3);
}

.add-icon { color: var(--text-muted); }

.quick-add-input {
  flex: 1;
  background: transparent;
  border: none;
  color: var(--text-primary);
  font-family: var(--font-sans);
  font-size: var(--text-base);
  outline: none;
}

.quick-add-input::placeholder { color: var(--text-muted); }

.quick-add-btn {
  background-color: var(--accent);
  color: #000;
  font-weight: 600;
  padding: var(--space-2) var(--space-5);
  border-radius: var(--radius-lg);
  font-size: var(--text-base);
  transition: opacity var(--dur-base) ease;
}

.quick-add-btn:hover:not(:disabled) { opacity: 0.85; }
.quick-add-btn:disabled { opacity: 0.4; cursor: not-allowed; }
```

- [ ] **Step 8.4 — Update `src/components/productivity/PomodoroTimer.css`** — token rename

Replace `var(--accent-cyan-dim)` → `var(--accent-dim)`, `var(--accent-cyan)` → `var(--accent)`, `var(--border-color)` → `var(--border-default)`:
```css
.pomodoro-widget {
  background-color: transparent;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: var(--space-2);
  transition: all var(--dur-slow) ease;
}

.pomodoro-widget.focus.running { background-color: var(--accent-dim); border-radius: var(--radius-md); padding: var(--space-2); }
.pomodoro-widget.break.running { background-color: var(--success-dim); border-radius: var(--radius-md); padding: var(--space-2); }

.pomodoro-header { display: flex; justify-content: space-between; align-items: center; }

.mode-switches {
  display: flex;
  gap: 2px;
  background: var(--bg-tertiary);
  padding: 2px;
  border-radius: var(--radius-md);
}

.mode-btn {
  background: transparent;
  border: none;
  color: var(--text-muted);
  padding: 2px var(--space-2);
  border-radius: var(--radius-sm);
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--dur-fast) ease;
}

.mode-btn:hover { color: var(--text-secondary); background: var(--bg-elevated); }
.mode-btn.active { color: var(--text-primary); background: var(--bg-primary); box-shadow: var(--shadow-sm); }

.pomodoro-time {
  font-family: var(--font-mono);
  font-size: 1.375rem;
  font-weight: 700;
  color: var(--text-primary);
  letter-spacing: 0.04em;
}

.pomodoro-widget.focus .pomodoro-time { color: var(--danger); }
.pomodoro-widget.break .pomodoro-time { color: var(--success); }

.pomodoro-task-select {
  width: 100%;
  background: transparent;
  border: 1px solid var(--border-default);
  color: var(--text-secondary);
  padding: var(--space-1) var(--space-2);
  border-radius: var(--radius-sm);
  font-size: var(--text-sm);
  font-family: var(--font-sans);
  outline: none;
  transition: border-color var(--dur-base) ease;
}

.pomodoro-task-select:focus { border-color: var(--border-focus); }
.pomodoro-task-select:disabled { opacity: 0.5; cursor: not-allowed; }

.pomodoro-controls { display: flex; gap: var(--space-2); justify-content: center; }

.ctrl-btn {
  background: var(--bg-tertiary);
  border: 1px solid var(--border-default);
  color: var(--text-primary);
  width: 30px;
  height: 30px;
  border-radius: var(--radius-full);
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  transition: background-color var(--dur-fast) ease;
}

.ctrl-btn:hover { background: var(--bg-elevated); }

.play-btn { background: var(--accent); color: #000; border: none; }
.play-btn:hover { opacity: 0.85; background: var(--accent); }
```

- [ ] **Step 8.5 — Verify** — Test all supporting components: open workspace modal, visit settings, use quick-add input, run pomodoro timer. Confirm: modal slides in from bottom, inputs glow on focus, timer digits sharp.

- [ ] **Step 8.6 — Commit**
```bash
git add src/components/workspace/WorkspaceModal.css src/app/settings/SettingsPage.css src/components/tasks/QuickAddTask.css src/components/productivity/PomodoroTimer.css
git commit -m "feat: supporting components — token rename, modal animation, input focus glow"
```

---

## Self-Review

### Spec Coverage

| Requirement | Covered by |
|-------------|-----------|
| All pages redesigned | Tasks 3, 4, 7, 8 |
| All emojis removed | Tasks 3, 5, 6, 7 (all 6 emoji locations) |
| All inline styles removed | Tasks 1, 2, 4 |
| Design token system | Task 1 |
| Sidebar active indicator | Task 2 |
| Semantic status colors | Tasks 1, 3, 4, 5, 6 |
| Inter font active | Task 1 (already loaded via next/font) |
| Dark/light both work | Task 1 (both themes defined) |
| No new dependencies | All tasks (confirmed) |

### Emoji coverage (all 6 locations)

| File | Line | Emoji | Fixed in |
|------|------|-------|----------|
| `KanbanBoard.tsx` | 120 | 🔗 | Task 5 |
| `TaskDetailPanel.tsx` | 211 | 🍅 | Task 6 |
| `TaskDetailPanel.tsx` | 222 | 🍅 | Task 6 |
| `page.tsx` | 121 | 🔗 | Task 3 |
| `page.tsx` | 219 | 📌 | Task 3 |
| `stats/page.tsx` | 46 | 🍅 | Task 7 |

### Placeholder scan

No TBD, TODO, "implement later", or vague steps present. All CSS blocks are complete.

### Token consistency

`--border-default`, `--accent`, `--accent-dim` used consistently from Task 1 onward. No references to removed `--border-color` or `--accent-cyan` in any Task 2+ code block.
